// Minimal backend integration client placeholder
// This file replaces the previous generated client export. Update with real client logic as needed.

export const backendClient: any = {
  // Placeholder auth object -- backend endpoints and `src/lib/api.ts` are the preferred API.
  auth: {
    // noop placeholders
    signOut: async () => {},
  },
};

// Usage in code: import { backendClient } from '@/integrations/backend/client'
